class biblioteca {
    constructor(titulo, autor, quantidade) {
        this._titulo = titulo;
        this.autor = autor;
        this.__quantidade = quantidade;
    }

    // Outros métodos da classe
}
